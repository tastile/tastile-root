{
  "_comment": "Bash PreToolUse guards run through .claude/hooks/hook-dispatch.mjs. It reads the event once and spawns only the guards whose subject matter appears in the command, so a plain `ls` no longer pays for two pwsh starts (~1.33s -> ~0.12s measured). Guard logic itself is unchanged and still regexes the whole command string.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bun",
            "args": [
              ".claude/hooks/hook-dispatch.mjs"
            ],
            "timeout": 1200,
            "statusMessage": "Checking Tastile command policy"
          }
        ]
      }
    ]
  }
}