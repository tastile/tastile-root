# Recurring-to-Source Status

This register is the resumable control plane. A row is not complete until its evidence is current for the recorded commit.

| Batch | Specification / scope | AT / UC | Owner | Exclusive scope | Depends on | Commit | Verification | Review | Next resume point |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0-control-plane | Plan Task 1; program controls and release gate | AT-UC: UC01-30 evidence control | Controller | `docs/implementation/recurring-to-source/**`, `scripts/orchestration/**` | None | `2107a6a`, `8f1749f` | Independent review approved; PowerShell parse and `verify-release.ps1 -WhatIf` passed | Approved; later root-glob regression fix uncommitted | Commit/review claim release + glob regression fix |
| A-source-scheduling | SourceTile-only new scheduling writer + workflow-type break phases (G-11) | AT SourceTile scheduling; UC01-30 as applicable | A | See `OWNERSHIP.md` | 0-control-plane | 9ea455b (G-11) -> 312bb5c (next-batch spec) -> d334468 (V1_028 production data fix: step=30min -> 20min + phase-based flow) | cargo fmt --all --check clean, cargo clippy --workspace --all-targets -D warnings clean at 9ea455b, cargo test -p domain --lib 198 passed 0 failed. **Production fix in d334468**: existing users' default 休憩 had step=30min, gap_min=10min, no v1_flow phases. The Step generator produced 30-min breaks at every 30-min interval. V1_028 migration: UPDATE step=20min + UPDATE gap_min=20min + INSERT v1_flow with phases for each existing default 休憩. Verified end-to-end: pre-V1_028 had 6 step=30min + 0 flows + 6 gap=10min; post-V1_028 has 0 step=30min + 6 flows + 0 gap=10min. New `at_default_break_workflow_upgrade` AT verifies the migration end-to-end. `migrate_run` CLI binary added for invoking migrations against any `TASTILE_DATABASE_URL`. | 2026-07-21T17:50:00Z | 21aabcc -> 82ba775 -> 9ea455b -> 312bb5c -> d334468 | Controller | Apply V1_028 to production DB by restarting the api/worker containers (or running `migrate_run`); the `Store::connect` path triggers it automatically. |
| B-http-evidence | Canonical Core HTTP contract and evidence harness | AT HTTP/multi-user; UC01-30 as applicable | B | See `OWNERSHIP.md` | A schema/DTO decisions | ecaeb9f (env-gate), 714ca27 (cross-owner), fe19ebb (live api+worker+db), fa62da8 (G-11 E2E) | `cargo check -p api --bin api`, `cargo test -p api --test openapi_schema` passed; live WSLC api+worker validated by EV-WSLC-20260721-3/4/10; cross-owner isolation OK at 714ca27 (owner A 200, owner B 404); auth-first status contract for legacy Recurring endpoints enforced and tested in unit (recurring_authorization_tests::*); numeric registry for Flow signal kinds deferred to v1.1 per spec | numeric Flow registry is a v1.1 spec issue (GAP-INVENTORY P1); legacy Recurring endpoints return 410 from /v1/recurrings/* (recurring_authorization_tests::*); unit tests pass |
| C-clients-wslc | Android typed DTO (V1CommandPayloads SourcePlanDefinitionPayload) | All SourceTile client cases + USECASE01-30 | C | See OWNERSHIP.md | B HTTP contract (closed at ecaeb9f) | 82ba775 (5/6 plan fields typed) -> 312bb5c (next-batch spec) -> a5b732c (Condition + Term AST typed mirror, 6/6 fully met) | Web E2E bearer migration complete (commits 2bc331b + d76534e + f62c7e9, bunx tsc 0 errors). Android typed plan AST landed in commit 82ba775 with 5 of 6 plan fields typed. Commit a5b732c closed the last 1/6 field by shipping a typed polymorphic mirror of the v1/05 Condition + Term + leaf sub-enums (`ConditionAstMirror.kt` in `tastile-android`). The custom KSerializers emit externally-tagged wire JSON via raw `JsonEncoder.encodeJsonElement(JsonObject(...))`, so the on-wire JSON byte-for-byte matches what Core emits per the v1/05 OpenAPI schema. `SourceTileApiContractTest.source_tile_create_and_update_share_the_canonical_payload_shape` constructs the typed `ConditionSchema.Any(emptyList())` and asserts the exact wire JSON. New `ConditionAstWireShapeTest` runs 16 tests covering each variant + round-trip. `gradle :app:testDebugUnitTest --rerun-tasks` reports 424 tests / 0 failures / 0 errors with JAVA_HOME=Microsoft-jdk-17.0.14.7-hostspot. The criterion "Android typed DTO" is now FULLY met. | 2026-07-21T16:30:00Z | 21aabcc -> 82ba775 -> a5b732c | Controller | n/a (criterion closed) |
| release | Release verification and evidence closure | All AT-UC, especially UC01-30 | Controller | `live-stack` claim | A, B, C batches | Pending | Run `verify-release.ps1` without `-WhatIf` only under live-stack claim | Release review pending | Attach artifact paths to `EVIDENCE.md` |

## Update rule

Append a new table row or revise the affected row only after a focused gate. Record a commit SHA, UTC timestamp, fixture owner, and artifact path in `EVIDENCE.md`; otherwise leave the result `UNPROVEN`.
