# Recurring-to-Source Status

This register is the resumable control plane. A row is not complete until its evidence is current for the recorded commit.

| Batch | Specification / scope | AT / UC | Owner | Exclusive scope | Depends on | Commit | Verification | Review | Next resume point |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0-control-plane | Plan Task 1; program controls and release gate | AT-UC: UC01-30 evidence control | Controller | `docs/implementation/recurring-to-source/**`, `scripts/orchestration/**` | None | `2107a6a`, `8f1749f` | Independent review approved; PowerShell parse and `verify-release.ps1 -WhatIf` passed | Approved; later root-glob regression fix uncommitted | Commit/review claim release + glob regression fix |
| A-source-scheduling | SourceTile-only new scheduling writer (Recurring writer path env-gated at b269a18) | AT SourceTile scheduling; UC01-30 as applicable | A | See `OWNERSHIP.md` | 0-control-plane | In progress | cargo check -p api + cargo check -p storage + cargo fmt --all --check passed locally; b269a18 env-gate (TASTILE_LEGACY_RECURRING_SEED default-off) stops new Recurring writes; ecaeb9f WSLC E2E on tastile-api-evidence-20260721-v3 confirms signup 200 and unchanged 休憩 row counts (1082/698/384/553 -> 1082/698/384/553) | Review found calendar-write, Flow-replacement, default-break proof gaps; default-break still seeded as v1_source_tile (kind=3) historically | Fix review findings; complete calendar-write parity with v1/14; finish default-break SourceTile replacement so DB has 0 legacy 休憩 rows; resume clippy and cargo test in fresh session (see EV-WSLC-20260721-4) |
| B-http-evidence | Canonical Core HTTP contract and evidence harness | AT HTTP/multi-user; UC01-30 as applicable | B | See `OWNERSHIP.md` | A schema/DTO decisions | Pending | `cargo check -p api --bin api`, `cargo test -p api --test openapi_schema` passed | Review found auth-order, numeric Flow wire, and owner/race gaps; auth-order partly fixed | Implement numeric registry only after specification decision; add owner/race HTTP fixture |
| C-clients-wslc | Android typed DTO (V1CommandPayloads) + Web E2E header removal | All SourceTile client cases + USECASE01-30 | C | See OWNERSHIP.md | B HTTP contract (closed at ecaeb9f) | In progress | Web env gate landed (cdd0bb4, EV-WSLC-20260721-6); Android SourcePlanDefinitionPayload still uses JsonObject/JsonArray (V1CommandPayloads.kt:298-318) and the typed Kotlin mirror of the v1/05 Condition AST (4 Condition + 10 Term variants + 5 sub-structures ReferenceDef/MetricDef/DecisionDef/FrameRuleDef + Completion with TimeRequirement/TaskDefinition) needs ~300-500 lines of new typed data classes with @Serializable + a wire-shape parity test against Core's serde output. Android Gradle build verification is required (host has JDK 17 but no Android SDK in this session) so this commit is deferred to a session with the Android build chain available. | 2026-07-21T03:05:53Z | 1d2dfd5 | Controller | commit cdd0bb4 (tastile-web) + planned: tastile-android V1CommandPayloads.kt + ConditionAst.kt (next session) |
| release | Release verification and evidence closure | All AT-UC, especially UC01-30 | Controller | `live-stack` claim | A, B, C batches | Pending | Run `verify-release.ps1` without `-WhatIf` only under live-stack claim | Release review pending | Attach artifact paths to `EVIDENCE.md` |

## Update rule

Append a new table row or revise the affected row only after a focused gate. Record a commit SHA, UTC timestamp, fixture owner, and artifact path in `EVIDENCE.md`; otherwise leave the result `UNPROVEN`.
