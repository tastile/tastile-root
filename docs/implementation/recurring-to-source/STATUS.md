# Recurring-to-Source Status

This register is the resumable control plane. A row is not complete until its evidence is current for the recorded commit.

| Batch | Specification / scope | AT / UC | Owner | Exclusive scope | Depends on | Commit | Verification | Review | Next resume point |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0-control-plane | Plan Task 1; program controls and release gate | AT-UC: UC01-30 evidence control | Controller | `docs/implementation/recurring-to-source/**`, `scripts/orchestration/**` | None | `2107a6a`, `8f1749f` | Independent review approved; PowerShell parse and `verify-release.ps1 -WhatIf` passed | Approved; later root-glob regression fix uncommitted | Commit/review claim release + glob regression fix |
| A-source-scheduling | SourceTile-only new scheduling writer + workflow-type break phases (G-11) | AT SourceTile scheduling; UC01-30 as applicable | A | See `OWNERSHIP.md` | 0-control-plane | b269a18 (env-gate), 9ea455b (G-11 phases) | cargo fmt --all --check clean, cargo clippy --workspace --all-targets -D warnings clean at 9ea455b, cargo test -p domain --lib 198 passed 0 failed including plan_uses_phases_for_workflow_break, plan_full_break_workflow_sequence, plan_resets_phases_at_gap_boundary; ecaeb9f WSLC env-gate signup E2E showed 休憩 row counts unchanged (1087); fa62da8 EV-WSLC-20260721-10 live SourceTile->Occurrence->Placement E2E on the G-11 build (db/api/worker all running) | Both 休憩 and 睡眠 Recurring writes are gated by the same default-off env var inside auth.rs::signup and migrations.rs V1_015 backfill; the G-11 workflow Flow is also gated; default-break SourceTile replacement at DB-zero over remaining 1087 legacy rows is a separate data-fix task; sleep-seed env-gate verification described in EV-WSLC-20260721-10 |
| B-http-evidence | Canonical Core HTTP contract and evidence harness | AT HTTP/multi-user; UC01-30 as applicable | B | See `OWNERSHIP.md` | A schema/DTO decisions | ecaeb9f (env-gate), 714ca27 (cross-owner), fe19ebb (live api+worker+db), fa62da8 (G-11 E2E) | `cargo check -p api --bin api`, `cargo test -p api --test openapi_schema` passed; live WSLC api+worker validated by EV-WSLC-20260721-3/4/10; cross-owner isolation OK at 714ca27 (owner A 200, owner B 404); auth-first status contract for legacy Recurring endpoints enforced and tested in unit (recurring_authorization_tests::*); numeric registry for Flow signal kinds deferred to v1.1 per spec | numeric Flow registry is a v1.1 spec issue (GAP-INVENTORY P1); legacy Recurring endpoints return 410 from /v1/recurrings/* (recurring_authorization_tests::*); unit tests pass |
| C-clients-wslc | Android typed DTO (V1CommandPayloads SourcePlanDefinitionPayload) | All SourceTile client cases + USECASE01-30 | C | See OWNERSHIP.md | B HTTP contract (closed at ecaeb9f) | 82ba775 (5/6 plan fields typed; completion.root still JsonElement) | Web E2E bearer migration complete (commits 2bc331b + d76534e + f62c7e9, bunx tsc 0 errors). Android typed plan AST landed in commit 82ba775: `SchedulePlanAst.kt` typed ReferenceDef / PlacementRule / NestingRule / MetricDef / DecisionDef / SchedulingPlanningDefinition / CompletionSchema (completion.root still JsonElement) / TimeRequirement etc.; `SourceTileWritePayload.plan` switched to the typed version. `SourceTileApiContractTest.source_tile_create_and_update_share_the_canonical_payload_shape` constructs the typed payload and asserts the exact wire JSON. `gradle :app:testDebugUnitTest --rerun-tasks` reports 408 tests / 0 failures / 0 errors with JAVA_HOME=Microsoft-jdk-17.0.14.7-hostspot. The remaining 1/6 field (completion.root, the recursive Condition AST) needs a custom KSerializer for externally-tagged unions -- the next batch has a written spec at `docs/implementation/recurring-to-source/NEXT-BATCH-android-condition-ast.md` (commit 312bb5c) with the corrected descriptor pattern and a `ConditionAstWireShapeTest` plan. | 2026-07-21T06:50:00Z | 21aabcc -> 82ba775 -> NEXT-BATCH spec at 312bb5c | Controller | Next batch: implement custom KSerializer per polymorphic level (Condition, Term, MomentTarget, MomentComparison, FactComparison, MetricComparison, LifeTarget), replace completion.root + the remaining JsonElement fields with typed mirrors; add ConditionAstWireShapeTest; rerun gradle :app:testDebugUnitTest |
| release | Release verification and evidence closure | All AT-UC, especially UC01-30 | Controller | `live-stack` claim | A, B, C batches | Pending | Run `verify-release.ps1` without `-WhatIf` only under live-stack claim | Release review pending | Attach artifact paths to `EVIDENCE.md` |

## Update rule

Append a new table row or revise the affected row only after a focused gate. Record a commit SHA, UTC timestamp, fixture owner, and artifact path in `EVIDENCE.md`; otherwise leave the result `UNPROVEN`.
