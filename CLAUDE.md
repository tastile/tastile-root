# Claude Code adapter

この workspace の canonical contract は `AGENTS.md` である。作業前に全文を読み、対象の
child repository と `.agents/skills/` へ routing すること。

Claude Code 固有の設定は `.claude/settings.json`、hook は `.claude/hooks/`、薄い Skill
adapter は `.claude/skills/` にある。project-wide の規則をこのファイルへ複製しない。
